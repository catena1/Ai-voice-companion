name: ai_voice_companion
description: AI Voice Companion App
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.0
  permission_handler: ^11.0.0
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  http: ^1.1.0

flutter:
  uses-material-design: true
